# Cheatsheet for all the commands I've learned in the book

Mice:
    $ date    # Show date
    $ cal   # Show calander
    cd ..   # To go backwards

ls commands:
    ls -l    # long format listing
    ls -a    # show hidden files too